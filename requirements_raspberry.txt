setuptools
dynamixel-sdk
pyserial
ultralytics

# Runtime dependencies are installed from system_requirements_raspberry.txt.
# OpenCV, NumPy, gpiozero and lgpio are intentionally installed with apt
# for Raspberry/ROS 2 compatibility.

# ultralytics (pulls in torch, CPU-only on the Pi) is needed by
# object_detector.py (rescue_bringup) to run the trained hazmat YOLO model —
# see pi_sensors.launch.py / logitech_vision.launch.py. Without it,
# object_detector silently falls back to the HSV color detector.
