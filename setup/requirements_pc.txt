setuptools
customtkinter
matplotlib
ultralytics

# Runtime dependencies are installed from system_requirements_pc.txt.
# OpenCV and NumPy are intentionally installed with apt for ROS 2 compatibility.

# ultralytics (pulls in torch) is needed for object_detector.py (hazmat/YOLO)
# and training/test_hazmat_camera.py. No apt package exists for it.
